#include <iostream>
using namespace std;

int main() {
    int yyyy, dd, mm;
    cin >> mm; cin.get();
    cin >> dd; cin.get();
    cin >> yyyy;
    cout << yyyy << "." << mm << "." << dd;
    return 0;
}